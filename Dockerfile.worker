ARG WORKER_BASE_IMAGE=epic-kiosk-worker-base:local
FROM ${WORKER_BASE_IMAGE}

WORKDIR /app

# 复制业务代码；基础依赖和浏览器已在 Dockerfile.worker-base 中预装
COPY app ./app
COPY worker.py ./

USER root

CMD ["python3", "worker.py"]
